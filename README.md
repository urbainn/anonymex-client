# Anonymex : client

Installer les dépendances :
```bash
npm i
```


Démarrer VITE en local :
```bash
npm run dev
``` 

> [!WARNING]
> Le client écoute le serveur sur l'addresse `localhost:3000` par défaut. Cette valeur peut être modifiée avec `URL_API_BASE` dans `api.ts`.